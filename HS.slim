initialize() {
	initializeMutationRate(0);
	initializeMutationType("m1", 0.5, "f", 0.0);
	initializeMutationType("m2", 0.5, "f", 0.0);
	initializeGenomicElementType("g1", c(m1, m2), c(0.5, 0.5));
	// Genome size is up for consideration
	initializeGenomicElement(g1, 0, 100000);
	// Simplest case: 4 equally sized cromossomes, with some pretty high recombination
	initializeRecombinationRate(c(5e-6, 0.5, 5e-6, 0.5, 5e-6, 0.5, 5e-6), c(24999, 25000, 49999, 50000, 74999, 75000, 100000));
}

// create a population of 500 individuals
1 late() {
	defineConstant("n_founders", 20);
	sim.readFromPopulationFile(filePath="/home/diogro/projects/HS_simulations/outputs/burnin.pop");
}

10050 early() {
	// set up pN to generate itself entirely from migrants
	sim.addSubpop(n_founders+1, 50000);
	rates = rep(1, n_founders);
	rates = rates/sum(rates);
	rates[0] = rates[0] + (1 - sum(rates));
	sim.subpopulations[n_founders].setMigrationRates(sim.subpopulations[0:(n_founders-1)], rates);
}
10050 late() {
	// remove the source subpopulations
	sim.subpopulations[n_founders].setMigrationRates(sim.subpopulations[0:(n_founders-1)], rep(0, n_founders));
	for (i in 0:(n_founders-1))
		sim.subpopulations[0].setSubpopulationSize(0);
}
10099 late() {
	// Introduce selection	
	mut = sim.mutationsOfType(m1);
	n_mut = length(mut);
	n_s = asInteger(n_mut);
	s = rbeta(n_s, 1, 40);
	for(i in 0:(n_s-1)){
		mut[i].setSelectionCoeff(s[i]);
	}
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_001.vcf");
}
10110 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_010.vcf");
}
10120 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_020.vcf");
}
10130 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_030.vcf");
}
10140 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_040.vcf");
}
10150 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_050.vcf");
}
10160 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_060.vcf");
}
10170 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_070.vcf");
}
10180 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_080.vcf");
}
10190 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_090.vcf");
}
10200 late() {
	sim.subpopulations[0].outputVCFSample(1000, filePath="~/projects/HS_simulations/outputs/HS_100.vcf");
}